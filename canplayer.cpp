#include "canplayer.h"

/*CANPlayer::CANPlayer(QObject *parent) : CANReader(parent)
{

}

void CANPlayer::open(QString filename){

    QFile input(filename);
    if (input.open(QIODevice::ReadOnly))
    {
       QTextStream in(&input);
       while (!in.atEnd())
       {
          QString line = in.readLine();

       }
       input.close();
    }


}*/
